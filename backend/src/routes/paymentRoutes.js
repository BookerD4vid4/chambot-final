/**
 * @deprecated These routes are decommissioned.
 * The /api/payment endpoints are no longer registered in app.js.
 */
const express = require("express");
const router = express.Router();
module.exports = router;
